{
  "name": "kordeon-website",
  "compatibility_date": "2026-07-06",
  // Assets-only Worker: the site is fully prerendered to static HTML, so there is
  // no `main` server entry — Cloudflare serves `dist/client` straight from the edge.
  "assets": {
    "directory": "./dist/client"
  }
}
